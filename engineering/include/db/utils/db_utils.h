/*
 * db_utils.h
 *
 * 数据库内核工具库 —— 公共头文件。
 * 包含向量索引依赖的数据结构。
 */

#ifndef DB_UTILS_H
#define DB_UTILS_H

/*
 * 注意：原 faiss_heap / faiss_minimax_heap / faiss_visited_table /
 * single_result_handler 等工具头已被删除，相关接口由新的 faiss_hnsw 模块
 * 内部实现。本头文件保留为空壳，向后兼容已包含此文件的源。
 */

#endif /* DB_UTILS_H */
