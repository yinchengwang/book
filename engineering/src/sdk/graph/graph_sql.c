/**
 * @file graph_sql.c
 * @brief 图模型 SQL 语句生成器（占位）
 *
 * 当前实现位于 graph.c 内（DDL/SQL 硬编码）；
 * 此文件为后续抽取 SQL 构建逻辑预留位置。
 */
#include "sdk/mmdb_graph.h"

/* 当前实现无需独立 SQL 生成器——所有 SQL 直接在 graph.c 中拼接。
 * 保留本文件作为占位，方便后续按模型拆分。 */