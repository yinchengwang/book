# 空间索引(R-Tree)

## 概述
演示 R-Tree 空间索引的原理：MBR、最小面积增长插入、矩形查询剪枝。

## 编译运行
```bash
make && ./test.exe
```

## 核心概念
- MBR（最小包围矩形）：每个节点维护一个包围所有子条目的矩形
- R-Tree 插入：选择最小面积增长子节点，叶节点满则分裂
- 节点分裂：按 x 中心排序，均分到两个节点
- 矩形查询与剪枝：MBR 不相交则跳过整个子树

## 与工程源码对照
参见 NOTES.md。