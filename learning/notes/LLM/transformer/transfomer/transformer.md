NNLM 模型: (Neural Network Language Model)


Word2Vec模型：
 - CBOW模型(Continuous Bag-of-Words): 使用周围的w(t + 2), w(t + 1), w(t - 2), w(t - 1)来预测当前词w(t)
 - skip-gram模型: 使用当前词w(t)预测周围词语


ELMo 模型: (Embeddings from Language Models)

BERT 模型:(Bidirectional Encoder Representations from Transformers)

Transformer 模型:
- 纯 Encoder(Encoder-only) 模型(BERT):
- 纯 Decoder(Decoder-only) 模型(GPT):
- Encoder-Decoder 模型(BART, T5):
