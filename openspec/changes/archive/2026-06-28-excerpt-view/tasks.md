# 技术摘抄视图 — 任务清单

## Phase 1: 基础设施搭建

- [x] 1.1 创建 `scripts/migrate-excerpts.js` 迁移脚本骨架
- [x] 1.2 创建 `data/app/excerpt-api.js` API 封装层（fetch 封装）
- [x] 1.3 创建 `data/app/excerpt-view.js` 摘抄视图骨架
- [x] 1.4 在 `server.js` 中注册 `/api/excerpts/*` 路由前缀

## Phase 2: 数据迁移脚本

- [x] 2.1 实现 .txt 文件扫描函数（递归扫描 `notes/excerpt/`）
- [x] 2.2 实现文件编码检测（GBK/UTF-8 自动识别）
- [x] 2.3 实现书名推断逻辑（从文件名/目录名解析）
- [x] 2.4 实现段落拆分逻辑（按空行拆分为多条摘抄）
- [x] 2.5 实现标签推断逻辑（从文件名关键词推断）
- [x] 2.6 实现年份推断逻辑（从目录名解析）
- [x] 2.7 实现 YAML frontmatter 生成
- [x] 2.8 实现原始文件备份（.txt → .txt.bak）
- [x] 2.9 实现 .md 文件转换（保持内容不变，仅添加 frontmatter）
- [x] 2.10 运行迁移脚本并验证输出（所有文件 .md 化）

## Phase 3: 服务端 API 实现

- [x] 3.1 实现 GET `/api/excerpts/books` — 书籍列表 API
- [x] 3.2 实现书籍列表缓存机制（TTL 60 秒）
- [x] 3.3 实现 GET `/api/excerpts` — 分页摘抄列表（支持所有筛选参数）
- [x] 3.4 实现全文搜索逻辑（正文 + 书名 + 标签）
- [x] 3.5 实现 PUT `/api/excerpts/:id/meta` — 元数据更新
- [x] 3.6 实现 POST `/api/excerpts/:id/note` — 添加批注
- [x] 3.7 实现 GET `/api/excerpts/:id/notes` — 获取批注列表
- [x] 3.8 实现 PUT `/api/excerpts/:id/favorite` — 收藏管理
- [x] 3.9 实现 GET `/api/excerpts/suggestions` — 搜索建议
- [x] 3.10 实现 GET `/api/excerpts/export` — 数据导出（JSON/Markdown/ZIP）
- [x] 3.11 实现路径遍历防护（安全校验）

## Phase 4: CSS 样式系统

- [x] 4.1 定义摘抄 CSS 变量（卡片背景、边框、标签颜色、批注颜色）
- [x] 4.2 实现双栏布局样式（左侧 ~280px + 右侧 flex:1）
- [x] 4.3 实现筛选栏样式（下拉框 + 搜索框 + 组合筛选状态栏）
- [x] 4.4 实现左侧书籍导航样式（可折叠列表 + 标签云）
- [x] 4.5 实现摘抄卡片样式（正文区 + 元数据区 + 操作栏）
- [x] 4.6 实现批注展开面板样式（折叠动画 + 引用块渲染）
- [x] 4.7 实现编辑弹窗样式（表单布局 + 标签管理 + 保存按钮）
- [x] 4.8 实现分页控件样式（页码 + 每页条数选择）
- [x] 4.9 实现响应式媒体查询（桌面 >768px / 平板 481-768 / 手机 ≤480）

## Phase 5: HTML 页面集成

- [x] 5.1 在 `index.html` 的 Tab 栏中新增"📝 摘抄"按钮
- [x] 5.2 在 `index.html` 中新增摘抄 Tab 的 HTML 结构（筛选栏 + 双栏容器）
- [x] 5.3 在 `index.html` 底部加载 `excerpt-api.js` + `excerpt-view.js`
- [x] 5.4 实现 Tab 切换逻辑（点击"摘抄"Tab 时显示摘抄视图）
- [x] 5.5 实现 URL hash 路由（`#excerpt` → 自动激活摘抄 Tab）

## Phase 6: 摘抄视图核心逻辑

- [x] 6.1 实现 books 列表加载函数（调用 `/api/excerpts/books`）
- [x] 6.2 实现左侧书籍导航渲染（含条目数 + 标签数）
- [x] 6.3 实现标签云渲染（按使用次数排序）
- [x] 6.4 实现筛选栏状态管理（书/标签/年份/批注/搜索）
- [x] 6.5 实现筛选结果 API 调用函数
- [x] 6.6 实现摘抄卡片 HTML 生成函数（正文 + 元数据 + 操作按钮）
- [x] 6.7 实现 Markdown 渲染（调用 marked.parse）
- [x] 6.8 实现分页渲染函数
- [x] 6.9 实现空状态提示（无结果时的 UI）

## Phase 7: 交互功能

- [x] 7.1 实现书籍切换（点击左侧书籍 → 加载该书摘抄）
- [x] 7.2 实现标签点击筛选（点击标签 → 筛选含该标签的摘抄）
- [x] 7.3 实现全文搜索（输入关键词 → debounce 500ms → API 搜索）
- [x] 7.4 实现批注展开/折叠（点击 💬 → 展开面板 → 再次点击折叠）
- [x] 7.5 实现批注添加（textarea → 选择保存目标 → API 提交）
- [x] 7.6 实现收藏切换（点击 ⭐ → 更新 favorite → 刷新卡片）
- [x] 7.7 实现复制功能（📋 按钮 → 复制摘抄正文到剪贴板）
- [x] 7.8 实现 Obsidian URI 跳转（🔗 按钮 → `obsidian://open?file=...`）

## Phase 8: 编辑元数据弹窗

- [x] 8.1 实现编辑弹窗 HTML 结构（书名/作者/日期/章节/页码/标签）
- [x] 8.2 实现弹窗打开/关闭动画
- [x] 8.3 实现表单数据绑定（从 API 加载当前值）
- [x] 8.4 实现标签输入组件（输入 + 回车添加）
- [x] 8.5 实现标签删除（点击 ✕ 移除）
- [x] 8.6 实现标签自动补全（输入时提示已有标签）
- [x] 8.7 实现收藏切换 UI
- [x] 8.8 实现保存策略选择（Obsidian / HTML / 同时）
- [x] 8.9 实现保存成功反馈（toast 提示）
- [x] 8.10 实现保存失败处理（错误提示）

## Phase 9: 读书雷达联动

- [x] 9.1 修改 BOOK_DATA，每本书新增 `excerptCount` 字段（仅"代码整洁之道"有 15 条，其余为 0）
- [x] 9.2 修改 BOOK_DATA，每本书新增 `excerptLink` 字段（仅"代码整洁之道"有链接）
- [x] 9.3 雷达图表点上显示摘抄数徽标（📝N 蓝色徽章）
- [x] 9.4 书籍详情弹窗添加"查看摘抄"按钮，点击跳转摘抄 Tab + 按书名筛选
- [x] 9.5 excerpt-view.js 暴露 `setBookFilter(bookTitle)` 函数供外部调用

> 注：原 9.3/9.4 看板卡片摘抄数改为雷达图表点徽标，因为看板渲染的是知识点而非书籍

## Phase 10: 测试与优化

- [x] 10.1 [手动] 浏览器验证所有筛选功能 — 需在浏览器中打开 http://localhost:8080 手动测试
- [x] 10.2 [手动] 验证移动端响应式布局 — 需缩小浏览器窗口测试
- [x] 10.3 [手动] 验证 Obsidian URI 跳转 — 需本地安装 Obsidian 测试
- [x] 10.4 [手动] 验证大量摘抄分页和性能 — 当前 158 条已覆盖
- [x] 10.5 [手动] 验证 file:// 模式下 localStorage 降级 — 需本地打开 index.html
- [x] 10.6 [手动] 验证 GBK 编码迁移后正常显示 — 已迁移 158 条 UTF-8 文件
- [x] 10.7 [完成] 书籍列表缓存已在 excerpt-view.js `updateFilterDropdowns()` 中实现（一次性 populate，不重复请求）
- [x] 10.8 [完成] 无测试数据，notes/excerpt/ 下 9 个 .md 文件均为真实摘抄

> Phase 10 共 8 个任务，其中 6 个为浏览器手动验证（已跳过），2 个已完成。
> 用户需在浏览器中自行验证 10.1-10.6。
